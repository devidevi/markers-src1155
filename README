
это смарт контракт, для игры.

для которого есть веб сайт с админкой который имееет доступ к контракту от имени "owner" чтоб добавлять динамически новые NFT,
также есть вторая часть сайта где пользователи играют в карты и если пользователь проиграиют они териалыот свои NFT карты,
это все реализована в смарт контракте. если углубица, там есть блокировка NFT токена на любое действие, также доступ к nft передается смарт контракту, пока игра не закончется.

для запуска требуется 
1) npm install
2) npm audit fix
3) заполнить .env файл,
4) запустить скрипт bash для деплой смарт контракта командой "bash scripts/deploy-contracts.sh" или "bash scripts/deploy-contracts-npx.sh"

для запуска тестов (mint) смарт контракта нужно в .env файлы записать дополнительные данные, "CONTRACT_ADDRESS", "OWNER_ADDRESS", "MNEMONIC" - от кошелька 


можно после настройки в ручную деплоить:

truffle migrate --network rinkeby
truffle deploy --network rinkeby
truffle run verify Markers --network rinkeby